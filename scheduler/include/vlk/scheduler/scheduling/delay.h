#include <chrono>

// delayed
// delayed_schedule
// nanoseconds granularity
// std::chrono::duration
// make a timer. really makes no sense????
void timer();

// run a delayed task
// nanoseconds
// milliseconds overload
//
void delay();
